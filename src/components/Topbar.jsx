import React from 'react';

const Topbar = () => {
  return (
    <div className="w-full h-12 bg-cover bg-center" style={{ backgroundImage: 'url("/assets/image1.png")' }}>
      {/* No text, just full image background */}
    </div>
  );
};

export default Topbar;
